#!/bin/bash

source $(dirname ${BASH_SOURCE})/config-common.sh

DISK=./kube/kube.vmdk
GUEST_ID=debian7_64Guest

NUM_MINIONS=${NUM_MINIONS-4}
INSTANCE_PREFIX=${KUBE_CLUSTER_NAME-kubernetes}
INSTANCE_START_INDEX=${INSTANCE_START_INDEX-0}
INSTANCE_END_INDEX=$((NUM_MINIONS - 1))

MASTER_NAME="${INSTANCE_PREFIX}-master-${INSTANCE_START_INDEX}"
MASTER_MEMORY_MB=1024
MASTER_CPU=1

MINION_NAMES=($(eval echo ${INSTANCE_PREFIX}-minion-{${INSTANCE_START_INDEX}..${INSTANCE_END_INDEX}}))
MINION_IP_RANGES=($(eval echo "10.244.{1..${NUM_MINIONS}}.0/24"))
MINION_MEMORY_MB=2048
MINION_CPU=1

PORTAL_NET="10.244.240.0/20"